# =============================================================================
# Global-Local Alignment Configuration
# =============================================================================

model:
  alignment:
    alpha_global: 0.5
    alpha_local: 0.5
    logit_temperature: 1.0
    learnable_weights: false


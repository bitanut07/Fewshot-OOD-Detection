# =============================================================================
# Text Refinement Module Configuration
# =============================================================================

model:
  text_refiner:
    # DiseaseTextRefiner parameters (see src/models/modules/text_refinement.py)
    text_dim: 512
    visual_dim: 512
    hidden_dim: 512
    num_heads: 8
    num_layers: 2
    dropout: 0.1
    alpha: 0.5
    trainable: true


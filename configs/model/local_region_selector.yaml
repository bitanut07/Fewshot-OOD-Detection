# =============================================================================
# Local Region Selector Configuration
# =============================================================================

model:
  local_region_selector:
    top_k: 4
    bottom_k: 4
    similarity_metric: "cosine"
    normalize_before_sim: true
    aggregate_method: "mean"

